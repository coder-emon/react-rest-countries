import React from 'react';

const Header = () => {
    return (
        <div className='text-center bg-primary'>
            <h1>This is the header of react app</h1>
        </div>
    );
};

export default Header;